terraform {
  backend "s3" {
    bucket       = "REPLACE_WITH_COMPANY_TERRAFORM_STATE_BUCKET"
    key          = "networking/dev-staging/openvpn.tfstate"
    region       = "eu-west-1"
    encrypt      = true
    use_lockfile = true
  }
}
